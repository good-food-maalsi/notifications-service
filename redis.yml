version: "3.7"

services:
  redis:
    image: redis:alpine
    container_name: notifications-redis
    ports:
      - "6379:6379"
    networks:
      - good-food
    volumes:
      - ./volumes/redis-data:/data

networks:
  good-food:
    external: true
